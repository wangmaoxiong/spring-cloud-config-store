
server:
  port: 8080
  
---
spring:
  profiles: develop

# 自定义属性值
feign:
  server:
    pt: frame-server
    basic: bgt-basic-server
---
spring:
  profiles: test

# 自定义属性值
feign:
  server:
    pt: frame-server-test
    basic: bgt-basic-server-test